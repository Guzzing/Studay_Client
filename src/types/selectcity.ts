export interface SidoType {
  sidoArr: string[]
  select: string
}
export interface SigunguType {
  sigunguArr: string[]
  select: string
}

export interface DongneType {
  dongneArr: string[]
  select: string
}
